server:
   port: 8765

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      
spring:
  application:
    name: service-feign  #像注册中心注册的服务名

feign.hystrix.enabled: true   # 代开feign的断路器
